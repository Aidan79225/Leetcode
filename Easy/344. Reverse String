class Solution {
public:
    string reverseString(string s) {
        int len = s.length();
        string result_string(s);
        for(int i=0;i<len;i++){
            result_string[i]=s[len-i-1];
        }
        return result_string;
    }
};
